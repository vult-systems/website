---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`grid md:auto-rows-[18rem] grid-cols-1 md:grid-cols-3 gap-4 max-w-[102rem] mx-auto ${className || ''}`}>
  <slot />
</div>
